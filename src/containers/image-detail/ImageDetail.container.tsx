import React from 'react';
import { SafeAreaView, ScrollView, Text } from 'react-native';

const ImageDetailContainer: React.FC<{}> = () => {
  return (
    <SafeAreaView>
      <ScrollView>
        <Text>This is image detail container</Text>
      </ScrollView>
    </SafeAreaView>
  );
};

export default ImageDetailContainer;
